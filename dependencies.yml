version: 2
dependencies:

  - type: js
    path: /
    manifest_updates:
      filters:
      - name: ".*"
        versions: "Y.Y.Y"
  
  - type: js
    path: style-guide

    settings:
      constraint_prefix: '^'
    manifest_updates:
      filters:
      - name: ".*"
        versions: "Y.Y.Y"
