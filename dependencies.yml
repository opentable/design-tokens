version: 2
dependencies:

  - type: js-npm
    path: /
    filters:
      - name: ".*"
        versions: "Y.Y.Y"
  
  - type: js-npm
    path: style-guide

    settings:
      constraint_prefix: '^'
    
    filters:
      - name: ".*"
        versions: "Y.Y.Y"
