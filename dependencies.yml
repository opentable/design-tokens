version: 2
dependencies:

  - type: js
    path: /
    filters:
      - name: ".*"
        versions: "Y.Y.Y"
  
  - type: js
    path: style-guide

    settings:
      constraint_prefix: '^'
    
    filters:
      - name: ".*"
        versions: "Y.Y.Y"
