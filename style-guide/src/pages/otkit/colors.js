import React from 'react';
import withLayout from '../../hocs/withLayout';
import Colors from '../../components/design-system-partials/otkit-colors.js';

export default withLayout(() => <Colors />, 'design-system-nav');
