props:
  # Used in ticketing modal of desktop restaurant profile
  # =============================================
  modal-margin-vertical:
    type: "size"
    value: "48px"

  # Used in ticketing modal of desktop restaurant profile
  # =============================================
  modal-max-width:
    type: "size"
    value: "672px"

  # Used in ticketing modal of desktop restaurant profile
  # =============================================
  modal-hero-fallback-background-color:
    value: "#492c6e"

global:
  platform: "desktop"
  type: "ticketing"
  category: "ticketing"
